Code contains Many functions to make code more easier.

It have Class State : which have state , Heap Map
It also have function equlas to check if two states are equal are not

Worklist() algorithm will compute all required analysis with help of process(unit) statement
for different kinds of defination statements

Alias function will check if two variables are alias or not.

